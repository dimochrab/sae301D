{% block head %}
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
{% endblock %}
{% block stylesheets %}
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@300;400;500;600;700&display=swap');

        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Montserrat', sans-serif;
        }

        body{
            background-color: #c9d6ff;
            background: linear-gradient(to right, #e2e2e2, #c9d6ff);
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            height: 100vh;
        }

        .container{
            background-color: #fff;
            border-radius: 30px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.35);
            position: relative;
            overflow: hidden;
            width: 955px;
            max-width: 100%;
            min-height: 552px;
        }

        .container p{
            font-size: 14px;
            line-height: 20px;
            letter-spacing: 0.3px;
            margin: 20px 0;
        }

        .container span{
            font-size: 12px;
        }
        .fa-chalkboard-teacher:before, .fa-chalkboard-user:before {
            content: "\f51c";
        }
        .container a{
            color: #333;
            font-size: 13px;
            text-decoration: none;
            margin: 15px 0 10px;
        }

        .container button{
            background-color: #512da8;
            color: #fff;
            font-size: 12px;
            padding: 10px 45px;
            border: 1px solid transparent;
            border-radius: 8px;
            font-weight: 600;
            letter-spacing: 0.5px;
            text-transform: uppercase;
            margin-top: 10px;
            cursor: pointer;
        }

        .container button.hidden{
            background-color: transparent;
            border-color: #fff;
        }

        .container form{
            background-color: #fff;
            display: flex;
            justify-content: center;
            flex-direction: column;
            padding: 0 40px;
            height: 100%;
        }

        .container input{
            background-color: #eee;
            border: none;
            margin: 8px 0;
            padding: 10px 15px;
            font-size: 13px;
            border-radius: 8px;
            outline: none;
        }
        input#email,input#password{
            background-color: #eee;
            border: none;
            margin: 8px 0;
            padding: 10px 15px;
            font-size: 13px;
            border-radius: 8px;
            outline: none;
            width: 100% ;
        }
        input#prenom ,input#nom{
            background-color: #eee;
            border: none;
            margin: 8px 0;
            padding: 10px 15px;
            font-size: 13px;
            border-radius: 8px;
            outline: none;
            width: 40% ;
        }

        .form-container{
            position: absolute;
            top: 0;
            height: 100%;
            transition: all 0.6s ease-in-out;
        }

        .sign-in{
            left: 0;
            width: 50%;
            z-index: 2;
        }

        .container.active .sign-in{
            transform: translateX(100%);
        }

        .sign-up{
            left: 0;
            width: 50%;
            opacity: 0;
            z-index: 1;
        }

        .container.active .sign-up{
            transform: translateX(100%);
            opacity: 1;
            z-index: 5;
            animation: move 0.6s;
        }

        @keyframes move{
            0%, 49.99%{
                opacity: 0;
                z-index: 1;
            }
            50%, 100%{
                opacity: 1;
                z-index: 5;
            }
        }

        .social-icons{
            margin: 20px 0;
        }

        .social-icons a{
            border: 1px solid #ccc;
            border-radius: 20%;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            margin: 0 3px;
            width: 40px;
            height: 40px;
        }

        .toggle-container{
            position: absolute;
            top: 0;
            left: 50%;
            width: 50%;
            height: 100%;
            overflow: hidden;
            transition: all 0.6s ease-in-out;
            border-radius: 150px 0 0 100px;
            z-index: 1000;
        }

        .container.active .toggle-container{
            transform: translateX(-100%);
            border-radius: 0 150px 100px 0;
        }

        .toggle{
            background-color: #512da8;
            height: 100%;
            background: linear-gradient(to right, #5c6bc0, #512da8);
            color: #fff;
            position: relative;
            left: -100%;
            height: 100%;
            width: 200%;
            transform: translateX(0);
            transition: all 0.6s ease-in-out;
        }

        .container.active .toggle{
            transform: translateX(50%);
        }

        .toggle-panel{
            position: absolute;
            width: 50%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 0 30px;
            text-align: center;
            top: 0;
            transform: translateX(0);
            transition: all 0.6s ease-in-out;
        }

        .toggle-left{
            transform: translateX(-200%);
        }

        .container.active .toggle-left{
            transform: translateX(0);
        }

        .toggle-right{
            right: 0;
            transform: translateX(0);
        }

        .container.active .toggle-right{
            transform: translateX(200%);
        }
        label{
            font-size: 14px;
            line-height: 20px;
            letter-spacing: 0.3px;
        }
        .formBlockInputsBlock {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            padding: 8px 16px;
            gap: 6px;
            background-color: #FFFFFF;
        }
        .formBlockTypeMail .formBlockInputsBlock input {
            max-width: 110px;
        }
        .formDefault {
            position: relative;
            width: fit-content;
            display: flex;
            flex-direction: column;
            padding: 16px;
            background-color: #dcdcdc;
            border: 2px solid #000;
            border-radius: 26px;
            min-width: 30vw;
        }

        .formDefault button {
            margin-top: 8px;
        }

        .formDefault .underButton {
            padding: 12px 16px;
            border-radius: 50px;
        }

        .formDefault .formBlockInputsBlock:focus-within {
            outline: 2px solid #000;
        }

        .formDefault .divTitre {
            width: fit-content !important;
        }

        .formDefault > div {
            display: flex;
            width: 100%;
            gap: 16px;
        }

        .formDefault input {
            background-color: #fff;
            border: none;
            width: 100%;
        }

        .formDefault input:focus {
            outline: none;
        }

        .formCenterBlock {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .formSVSBlock {
            width: 100%;
            display: grid !important;
            grid-template-columns: 1fr 1fr;
            gap: 16px !important;
        }

        .formBlockLabel {
            display: flex;
            align-items: center;
            gap: 8px;
            font-weight: 600;
            text-transform: uppercase;
        }

        /* Profil */
        .formBlockTypeRadio {
            position: relative;
            display: flex;
            flex-direction: column;
            gap: 10px; /* Remplace var(--grup-2) */
        }

        .formBlockTypeRadio {
            text-transform: uppercase;
        }

        .formBlockTypeRadio .formBlockInputsBlock {
            display: flex;
            flex-direction: row;
            flex-wrap: wrap;
            align-items: center;
            justify-content: center;
            gap: 8px;
            min-width: 340px;
        }

        .formBlockTypeRadio .formBlockInputsBlock:focus-within {
            outline: none;
        }

        .formBlockTypeRadio input {
            position: absolute;
            opacity: 0;
        }

        .formBlockTypeRadio label {
            flex-grow: 1;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 8px; /* Remplace var(--grup-1) */
            padding: 10px 16px; /* Remplace var(--grup-1) et var(--grup1) */
            font-weight: 600;
            background-color: #FFFFFF; /* Remplace var(--blanc) */
            border-radius: 50px;
            cursor: pointer;
            outline: rgba(0, 0, 0, 0.1) 1px solid; /* Remplace var(--noirTransparent) */
        }

        .formBlockTypeRadio label:hover {
            background-color: #512da8;
            color: #FFFFFF;
        }

        .formBlockTypeRadio input:checked + label {
            background-color: #512da8;
            color: #FFFFFF;
            outline: none;
        }

        /* Mail */
        .formBlockTypeMail {
            display: flex;
            flex-direction: column;
        }

        .formBlockTypeMail .formBlockLabel {
            display: flex;
            align-items: center;
            gap: 8px; /* Remplace var(--grup-1) */
        }

        .formBlockTypeMail .formBlockInputsBlock {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            gap: 6px;
            background-color: #FFFFFF;
            min-width: 446px;
        }

        .formBlockTypeMail .formBlockInputsBlock input {
            border-bottom: rgba(0, 0, 0, 0.1) 1px solid; /* Remplace var(--noirTransparentPlus) */
        }

        .formBlockTypeMail .formBlockInputsBlock input:focus {
            border-bottom: rgba(0, 0, 0, 0.5) 1px solid; /* Remplace var(--noirTransparent) */
        }

        .formBlockTypeMail .formBlockInputsBlock input {
            max-width: 110px;
        }

        .formBlockTypeMail .formBlockInputsBlock p {
            color: rgba(0, 0, 0, 0.5); /* Remplace var(--noirTransparent) */
        }

        .formBlockTypeMail .formBlockInputsBlock > div {
            position: relative;
        }

        .formBlockTypeMail .formBlockInputsBlock > div p:nth-child(2) {
            top: 0;
            position: absolute;
        }

        /* Text */
        .formBlockTypeText {
            display: flex;
            flex-direction: column;
            width: 100%;
            gap: 16px;
        }

        .formBlockTypeText .formBlockInputsBlock {
            position: relative;
            display: flex;
            flex-direction: row;
            gap: 16px;
            background-color: #FFFFFF;
        }

        .formBlockTypeText input {
            width: 100%;
        }

        .formBlockTypeText .afficherMDP {
            position: relative;
            display: flex;
        }

        .afficherMDP .fa-eye,
        .afficherMDP .fa-eye-slash,
        .afficherMDPBasic .fa-eye,
        .afficherMDPBasic .fa-eye-slash {
            position: absolute;
            z-index: 50;
            right: 0;
            height: 100%;
            width: 16px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
        }

        .afficherMDP input + label .fa-eye,
        .afficherMDPBasic input + label .fa-eye {
            display: none;
        }

        .afficherMDP input:checked + label .fa-eye,
        .afficherMDPBasic input:checked + label .fa-eye {
            display: flex;
        }

        .afficherMDP input:checked + label .fa-eye-slash,
        .afficherMDPBasic input:checked + label .fa-eye-slash {
            display: none;
        }

        .afficherMDP input {
            position: absolute;
            opacity: 1;
        }

        .afficherMDPBasic {
            position: relative;
            display: flex;
            width: fit-content;
            padding-right: 8px;
            white-space: nowrap;
        }

        .afficherMDPBasic input {
            opacity: 0;
        }

        /* Text Area */
        .formBlockTypeTextArea {
            display: flex;
            flex-direction: column;
        }

        .formBlockTypeTextArea .formBlockInputsBlock {
            position: relative;
            display: flex;
            flex-direction: row;
            gap: 16px; /* Remplace var(--grup-2) */
            padding: 16px 8px; /* Remplace var(--grup-2) et var(--grup) */
            background-color: #FFFFFF; /* Remplace var(--blanc) */
            border-radius: 16px;
            outline: rgba(0, 0, 0, 0.1) 1px solid; /* Remplace var(--noirTransparent) */
        }

        .formBlockTypeTextArea textarea {
            appearance: none;
            background-color: transparent;
            border: none;
            width: 100%;
            resize: none;
            outline: none;
        }

        .formDefault label > span,
        .formDefault p > span {
            color: red;
        }
        /* --- --------- */
        /* --- POPUP --- */
        /* --- ----- --- */

        /* FAIRE DISPARAITRE LES BOUTONS PLAN ET SATELLITE DE LA MAP */
        .gm-style-mtc {
            display: none !important;
        }

        /* POPUP */
        .popup {
            display: none;
            position: fixed;
            z-index: 1;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            overflow: auto;
            background-color: rgba(0, 0, 0, 0.7); /* Remplacez par votre couleur de fond */
            padding: 5%;
        }

        .popup-content {
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            position: relative;
            display: flex;
            flex-direction: column;
            gap: 20px; /* Remplacez par votre espacement */
            background-color: #ddd; /* Remplacez par votre couleur de fond */
            padding: 15px; /* Remplacez par votre espace intérieur */
            border: 2px solid black; /* Remplacez par votre couleur de bordure */
            max-width: 400px;
            min-height: 40vh;
            max-height: 539px;
            border-radius: 20px; /* Remplacez par votre rayon de bordure */
        }

        .close {
            position: absolute;
            right: 3%;
            top: 3%;
            color: black; /* Remplacez par votre couleur */
            float: right;
            font-size: 26px;
            font-weight: bold;
        }

        .close:hover,
        .close:focus {
            color: red; /* Remplacez par votre couleur au survol */
            text-decoration: none;
            cursor: pointer;
        }


        .cookie-popup p {
            font-size: 20px; /* Remplacez par votre taille de police */
        }

        .cookie-popup div {
            width: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px; /* Remplacez par votre espacement */
            flex-wrap: wrap;
        }

        .cookie-popup div .btn:nth-child(1):hover,
        .cookie-popup div .btn:nth-child(2):hover {
            background-color: yellow; /* Remplacez par votre couleur au survol */
            outline: none;
        }

        .cookie-popup div .btn:nth-child(2) {
            background-color: transparent;
            outline: black 2px solid; /* Remplacez par votre couleur de bordure */
            color: black; /* Remplacez par votre couleur */
        }

        .cookie-popup.show,
        .erreur-popup.show,
        .info-popup.show {
            bottom: 20px;
        }

        .erreur-popup {
            position: fixed;
            bottom: -100%;
            left: 50%;
            transform: translateX(-50%);
            padding: 20px 30px; /* Remplacez par votre espace intérieur */
            background-color: #e85b43; /* Remplacez par votre couleur de fond */
            border: 2px solid #e85b43; /* Remplacez par votre couleur de bordure */
            color: white; /* Remplacez par votre couleur */
            z-index: 9999;
            transition: bottom 0.5s ease-in-out;
            border-radius: 20px; /* Remplacez par votre rayon de bordure */
        }

        .erreur-popup .close {
            color: black; /* Remplacez par votre couleur */
        }

        .erreur-popup .close:hover,
        .erreur-popup .close:focus {
            color: white; /* Remplacez par votre couleur au survol */
        }

        .info-popup {
            position: fixed;
            bottom: -100%;
            left: 50%;
        }


    </style>


{% endblock %}

{% block body %}
    <body>

    <div class="container" id="container" >

        <div class="form-container sign-up">
            <form  method="POST">
                <h1>S'INSCRIRE</h1>
                <!-- <div class="formCenterBlock"> -->
                <div class="formBlockTypeRadio">
                    <div class=" formBlockLabel">
                        <i class="fas fas fa-regular fa-user"></i>
                        <p>COMPTE</p>
                    </div>
                    <div class="formBlockInputsBlock">
                        <div>
                            <input type="radio" name="statut" value="Etudiant" id="etudiant_signup" class="formInputCellInput" required />
                            <label for="etudiant_signup"><i class="fas fas fa-solid fa-graduation-cap"></i>Etudiant</label>
                        </div>
                        <div>
                            <input type="radio" name="statut" value="Professeur" id="prof_signup" class="formInputCellInput" required/>
                            <label for="prof_signup"><i class="fas fas fa-solid fa-chalkboard-user"></i>Professeur</label>
                        </div>
                    </div>
                </div>
                <!-- </div> -->
                <div class="formBlockTypeMail">
                    <div class="formBlockLabel">
                        <i class="fas fas fa-regular fa-envelope"></i>
                        <label>Adresse mail URCA</label>
                    </div>

                    <div class="formBlockInputsBlock">
                        <input type="text" placeholder="prénom" name="prenom" id="prenom_signup" required />
                        <p>.</p>
                        <input type="text" placeholder="nom" name="nom" id="nom_signup" required />
                        <div>
                            <p id="afficheEtudiant_signup">@etudiant.univ-reims.fr</p>
                            <p id="afficheProf_signup" style="opacity: 0">@univ-reims.fr</p>
                        </div>
                    </div>
                </div>
                <div class="formBlockTypeText">
                    <div class="formBlockLabel">
                        <i class="fas fa-solid fa-lock"></i>
                        <label for="password1_signup">Mot de passe</label>
                    </div>
                    <div class="formBlockInputsBlock">
                        <!-- Change the field type to "password" -->
                        <input type="password" id="password1_signup" name="mdp" required />
                        <div class="afficherMDP">
                            <input type="checkbox" id="revealPassword_signup" onclick="togglePasswordVisibilitySignup()"  >
                            <label for="revealPassword_signup">
                                <i class="fas fa-regular fa-eye"></i>
                                <i class="fas fa-regular fa-eye-slash"></i>
                            </label>
                        </div>
                    </div>
                </div>
                <button>JE M'INSCRIS</button>
            </form>
        </div>

        <div class="form-container sign-in">
            <form  method="POST">
                <h1>SE CONNECTER</h1>

                <!-- <div class="formCenterBlock"> -->
                <div class="formBlockTypeRadio">
                    <div class=" formBlockLabel">
                        <i class="fas fa-regular fa-user"></i>
                        <p>COMPTE</p>
                    </div>
                    <div class="formBlockInputsBlock">
                        <div>
                            <input type="radio" name="statut" value="Etudiant" id="etudiant_signin" class="formInputCellInput" required />
                            <label for="etudiant_signin"><i class="fas fa-solid fa-graduation-cap"></i>Etudiant</label>
                        </div>
                        <div>
                            <input type="radio" name="statut" value="Professeur" id="prof_signin" class="formInputCellInput" required />
                            <label for="prof_signin"><i class="fas fa-solid fa-chalkboard-user"></i>Professeur</label>
                        </div>
                    </div>
                </div>
                <!-- </div> -->
                <div class="formBlockTypeMail">
                    <div class="formBlockLabel">
                        <i class="fas fa-regular fa-envelope"></i>
                        <label>Adresse mail URCA</label>
                    </div>

                    <div class="formBlockInputsBlock">
                        <input type="text" placeholder="prénom" name="prenom" id="prenom_signin" required />
                        <p>.</p>
                        <input type="text" placeholder="nom" name="nom" id="nom_signin" required />
                        <div>
                            <p id="afficheEtudiant_signin">@etudiant.univ-reims.fr</p>
                            <p id="afficheProf_signin" style="opacity: 0">@univ-reims.fr</p>
                        </div>
                    </div>
                </div>
                <div class="formBlockTypeText">
                    <div class="formBlockLabel">
                        <i class="fas fa-solid fa-lock"></i>
                        <label for="password1_signin">Mot de passe</label>
                    </div>
                    <div class="formBlockInputsBlock">
                        <input type="password" id="password1_signin" name="mdp" required />
                        <div class="afficherMDP">
                            <input type="checkbox" id="revealPassword_signin" onclick="togglePasswordVisibilitySignin()">
                            <label for="revealPassword_signin">
                                <i class="fas fa-regular fa-eye"></i>
                                <i class="fas fa-regular fa-eye-slash"></i>
                            </label>
                        </div>
                    </div>
                </div>
                <div class="formCenterBlock">
                    <button type="submit" value="Envoyer" class="btn" onclick="validateform()"><i class="fas fa-solid fa-arrow-right-to-bracket"></i>SE CONNECTER</button>
                </div>
            </form>
        </div>
        <div class="toggle-container">
            <div class="toggle">
                <div class="toggle-panel toggle-left">
                    <h1>Docline</h1>
                    <p>Inscrivez-vous pour consulter vos rendus de travaux</p>
                    <button class="hidden" id="login">SE CONNECTER</button>
                </div>
                <div class="toggle-panel toggle-right">
                    <h1>Docline</h1>
                    <p>Connectez-vous pour consulter vos rendus de travaux</p>
                    <button class="hidden" id="register">JE M'INSCRIS</button>
                </div>
            </div>
        </div>
    </div>
    <div id="erreurPopup" class="erreur-popup">
        <span class="close" onclick="closePopup('erreurPopup')">×</span>
        <p>Désolé, le login saisi n'existe pas !</p>
    </div>
    </body>



{% endblock %}

    {% block javascripts %}
        <script src="{{ asset('js/script.js') }}"></script>
        <script src="{{ asset('js/info_erreur.js') }}"></script>
        <script src="https://kit.fontawesome.com/ebc72dbf59.js" crossorigin="anonymous"></script>
    {% endblock %}